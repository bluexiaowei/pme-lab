# PME 呼吸检测数据上报格式

> 本文由设备侧协议说明整理而来，供 BLE 联调与 `:pme-sdk` 对照。  
> **非临床用途**；完整免责见 [README · 声明与免责](README.md#声明与免责)。

## 目录

1. [术语](#1-术语)
2. [通信接口](#2-通信接口)
3. [消息模型](#3-消息模型)
4. [消息格式](#4-消息格式)
5. [命令一览](#5-命令一览)
6. [与本仓库实现的对照](#6-与本仓库实现的对照)

---

## 1. 术语

| 名称 | 含义 |
|------|------|
| RMS | 呼吸监测控制板（协议文档中的设备称呼） |
| ETCO₂ | 呼气末二氧化碳浓度 |
| FiCO₂ / InsCO₂ | 吸入气二氧化碳浓度 |
| SpO₂ | 脉搏血氧饱和度 |
| PEF | 峰值呼气流量 |
| FEV1 | 第一秒用力呼气容积（用力呼气第一秒内的呼出气量） |
| FVC | 用力肺活量 / 用力呼气容积 |
| FEV1/FVC | 第一秒率（FEV1 占 FVC 的百分比） |
| MEF75 | 用力呼出 75% FVC 时的瞬时流量 |
| MEF50 | 用力呼出 50% FVC 时的瞬时流量 |
| MEF25 | 用力呼出 25% FVC 时的瞬时流量 |
| MMEF | 最大中期呼气流量（呼出气量处于 25%～75% 肺活量阶段的平均流量） |
| cuff pressure | 血压袖带压 |
| diastolic blood pressure | 舒张压 |
| systolic blood pressure | 收缩压 |
| mean blood pressure | 平均动脉压 |

---

## 2. 通信接口

RMS 呼吸监测控制板通过 **蓝牙（BLE GATT）** 对外通信。

本仓库实测使用的服务 / 特征 UUID：

| 角色 | UUID |
|------|------|
| Service | `69400001-b5a3-f393-e0a9-e50e24dcca99` |
| Notify（从机→主机） | `69400002-b5a3-f393-e0a9-e50e24dcca99` |
| Write（主机→从机） | `69400003-b5a3-f393-e0a9-e50e24dcca99` |

---

## 3. 消息模型

- 控制板为 **从设备**，外部蓝牙设备为 **主设备**。
- 消息类型：请求（指令）、通知、应答。
- 需要应答的命令：收到后应回 ACK；ACK 的 **命令 ID 与请求相同**，参数为 **请求帧的帧序号**（4 字节小端）。

### 重传与容错

| 规则 | 说明 |
|------|------|
| 超时 | 发送方等待约 **200 ms** 无应答则重发 |
| 重试 | 最多再发 **2 次**；仍失败则通信异常 |
| 失败应答 | 收到失败应答应立即重发 |
| 坏帧 | 格式不符或校验失败的帧应 **忽略** |

---

## 4. 消息格式

一条消息 = **固定头** + **数据区** + **CRC16**。

文档原文称「固定 16 字节头」：指进入参数 buffer 之前的部分（12 字节固定头 + 4 字节数据区头）。

### 4.1 字段布局（小端）

| 偏移 | 长度 | 字段 | 说明 |
|------|------|------|------|
| 0 | 1 | 头标志 1 | 固定 `0x55` |
| 1 | 1 | 头标志 2 | 固定 `0xAA` |
| 2 | 2 | dataLen | 之后数据长度（**不含 CRC**）= `4 + params.length` |
| 4 | 1 | 源地址 | 主机 `0x00`，从机 `0x01` |
| 5 | 1 | 目的地址 | 同上 |
| 6 | 4 | 帧序号 | 每发一帧 +1 |
| 10 | 2 | 帧状态 | 见下表 |
| 12 | 1 | 产品 ID | 本项目 `0x01` |
| 13 | 1 | 协议类型 | `0` |
| 14 | 2 | 命令 ID | 见第 5 章 |
| 16 | 0～124 | 消息参数 | 变长 |
| 末尾 | 2 | CRC16 | 对「帧头～参数」计算（不含 CRC 自身） |

总长 = `12 + dataLen + 2`。

### 4.2 帧状态

| 值 | 含义 |
|----|------|
| `0x0002` | 请求 |
| `0x0004` | 应答 |
| `0x0008` | 通知 |

### 4.3 应答示例

请求（摘自原文）：

```text
55 AA 0A 00 01 02 2F 00 00 00 02 00 02 00 0A 10 03 64 00 80 DE 00 5D 16
```

- dataLen = `0x000A`
- 帧序号 = `0x0000002F`
- 命令 ID = `0x100A`

主机应答：

```text
55 AA 08 00 02 01 66 14 00 00 04 00 02 00 0A 10 2F 00 00 00 80 12
```

- 状态 = `0x0004`（应答）
- 命令 ID 同为 `0x100A`
- 参数 = 请求帧序号 `0x0000002F`

---

## 5. 命令一览

标题括号内为命令 ID。除特别说明外，**需要应答：是**。

### 5.1 设备信息（`0x1000`）

- **方向**：从机 → 主机

| 字节数 | 名字 | 描述 |
|--------|------|------|
| 16 | 序列号 | 字符串 |
| 6 | 硬件版本 | 字符串 |
| 18 | 软件版本 | 字符串，例如 `V1.0.0.1` |

### 5.2 设备状态（`0x1001`）

- **方向**：从机 → 主机

| 字节数 | 名字 | 描述 |
|--------|------|------|
| 1 | 电池状态 | 见下表 |
| 1 | 蓝牙连接状态 | `0` 已连接；`1` 未连接 |

**电池状态**

| 值 | 含义 |
|----|------|
| 0～4 | level 4 … level 0（电量档） |
| 5～9 | charging 0 … charging 4 |
| 10 | charge complete |
| 11～15 | charge fault 0 … charge fault 4 |
| 16 | ac charge fault |

### 5.3 蓝牙广播名称（`0x1100`）

- **方向**：从机 → 主机

| 字节数 | 名字 | 描述 |
|--------|------|------|
| N | 蓝牙广播名称 | 字符串，长度 ≤ 26 |

### 5.4 病人信息（`0x2000`）

- **方向**：从机 → 主机（设备上报；主机以接收展示为主）  
- **实验**：本 App「控制」页可选手写下发，固件可能忽略

| 字节数 | 名字 | 描述 |
|--------|------|------|
| 12 | 病人编号 | 字符串 |
| 1 | 性别 | `0` 男；`1` 女 |
| 1 | 类型 | `0` 成人；`1` 新生儿；`2` 儿童 |
| 2 | 身高 | > 0，单位 cm |
| 2 | 体重 | > 0，单位 kg |
| 2 | 年龄 | |
| 12 | 数据 ID | 字符串（如 11 位手机号） |

合计参数 **32 字节**。

### 5.5 生理数据（`0x2001`）

- **方向**：从机 → 主机  
- 多数字段为 **2 字节无符号小端整数**；部分字段在展示层需缩放（见第 6 章）

| 字节数 | 名字 | 描述 | 说明 |
|--------|------|------|------|
| 12 | 数据 ID | 字符串 | |
| 2 | ETCO₂ | 有效范围约 25.0～50.0 | 实时；无效填 `0xFF`（见下） |
| 2 | FiCO₂ / InsCO₂ | 整型 | |
| 2 | breath rate | 呼吸率 | |
| 2 | SpO₂ | 有效范围约 90～100 | |
| 2 | pulse rate | 单位 bpm | |
| 2 | PEF | L/min，约 0～850 | 保留上次有效值；断开可上报无效 |
| 2 | FEV1 | L，约 0～10 | 同上 |
| 2 | FVC | L，约 0～10 | |
| 2 | MEF75 | L/s，约 0～14 | |
| 2 | MEF50 | L/s，约 0～14 | |
| 2 | MEF25 | L/s，约 0～14 | |
| 2 | MMEF | L/s，约 0～14 | |
| 2 | FEV1/FVC | 百分数相关 | |
| 2 | cuff pressure | mmHg | 实时；无效填无效值 |
| 2 | diastolic BP | mmHg，约 50～90 | |
| 2 | systolic BP | mmHg，约 90～160 | |
| 2 | mean BP | mmHg，约 60～110 | |
| 2 | heart rate | 约 50～120 | |
| 2 | temperature | 约 35～38 | |

**无效值**：原文对两字节字段写作 `0xFF`；按字段宽度实际应为 **`0xFFFF`**。无更新时主机侧宜保留上次有效值（本仓库 `PmePhysioData.mergePreserve`）。

参数区合计至少 **50 字节**（12 + 19×2）。

### 5.6 建链 / 保活（联调补充，原文未单列）

实机联调中主机侧还会使用：

| 命令 ID | 名称 | 说明 |
|---------|------|------|
| `0x8001` | 建链 | 订阅 Notify 后由主机发起 |
| `0x8002` | 保活 | 约每 2 s 发送 |

---

## 6. 与本仓库实现的对照

| 主题 | 协议原文 / 约定 | `:pme-sdk` |
|------|-----------------|------------|
| 粘包解析 | 忽略坏帧 | `PmeCodec.feed` |
| 病人信息 | 32 字节布局 | `PmeProtocol.buildPatientInfoParams` / `parsePatientInfo` |
| 生理数据缩放 | 原文多为「整型」 | ETCO₂ / FiCO₂ / MEF* / FEV1/FVC% / 体温 ÷10；FEV1 / FVC ÷100（对齐实机） |
| 无效字段 | 写 `0xFF` | 按 `0xFFFF` 判空 |
| 设备信息 `0x1000` | 16+6+18 结构化 | `PmeDeviceInfo`（序列号 / 硬件 / 软件） |
| 设备状态 `0x1001` | 电池档 + 蓝牙状态 | `PmeDeviceStatus.batteryLabel` / `btLabel` |
| 广播名 `0x1100` | 字符串 ≤26 | `PmeBleName` |

实现入口：`PmeClient`；联调 UI：「控制」页展示设备上报的 `0x2000`，主机下发仅为实验选项。

---

## 修订说明

- 去掉 Word/PDF 导出残留的超宽空白列与内嵌 JPEG（原为公式截图等）。
- 统一标题层级与表格；补全 GATT UUID、建链/保活与 SDK 对照。
- 术语表中由图片表示的名称还原为 ETCO₂ / FiCO₂ / SpO₂。
